export const saves = {
  autosave: {
    enabled: true,
    interval: "3s",
  },
  path: "saves",
  world: "world",
};

export const modloader = {
  enabled: true,
  path: "mods",
};

export const server = {
  enabled: true,
  port: 3000,
  webapp: { enabled: true, port: 8080 },
};
